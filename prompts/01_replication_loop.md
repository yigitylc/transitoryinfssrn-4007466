# Prompt 01 — Paper replication loop

Proceed with the smallest useful paper-replication step from your approved plan.

Rules:

- Keep paper replication separate from live-safe signal mode.
- Do not redesign the dashboard yet unless necessary.
- Add or update tests for methodology-sensitive logic.
- Run `pytest` and `python -m compileall src app scripts`.

End with the standard handoff format from `CLAUDE.md`.

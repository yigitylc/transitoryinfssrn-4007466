# Prompt 00 — Read-only audit

You are inside my Transitory Inflation Macro Research project.

Act as a macro research engineer and quant/macro trader. Do **read-only audit only**. Do not edit files yet.

Tasks:

1. Inspect the project tree.
2. Read `README.md`, `CLAUDE.md`, `AGENTS.md`, and the docs in `docs/`.
3. Inspect `src/`, `app/`, `scripts/`, and `tests/`.
4. Compare the scaffold to the reference paper in `references/ssrn-4007466.pdf`.
5. Identify what is implemented, what is placeholder, and what is missing.
6. Ask me before proceeding if anything is ambiguous.

End with:

- proposed first implementation step,
- files you expect to touch,
- open questions,
- checks you will run after edits.

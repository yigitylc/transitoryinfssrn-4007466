# Prompt 02 — Live macro signal loop

Now improve the live-safe macro signal layer.

Focus on:

- shifted rolling or expanding baselines,
- clear live-safe/ex-post labels,
- current TINF readings and percentiles,
- interpretation that is useful for macro view.

Do not overbuild visuals. Choose the simplest dashboard additions that clarify the signal.

Run checks and end with the standard handoff format.

# Decision Log

Use this file to record research decisions that affect results.

| Date | Decision | Reason | Alternatives considered | Owner |
|---|---|---|---|---|
| 2026-06-09 | Keep replication and live-signal modes separate | Avoid mixing ex-post and real-time interpretations | Single blended mode | User/agent |
| 2026-06-09 | Named sample modes: `paper_replication` (1982-01..2021-07), `live_dashboard` (1982-01..latest, Streamlit default), `max_history` (all FRED). Loader fetches a 12-month YoY warm-up before `start_date`; `run_paper_replication.py` now slices at load time, before the full-sample baseline | Make date handling explicit; remove post-2021 lookahead from the paper baseline; YoY defined from sample start | Free-form date inputs; slice after features; no warm-up buffer | User/agent |
| 2026-06-09 | T-bill control switched to `TB3MS` (3-month, since 1934) as column `tbill_3m`; the old `TB1MS` id returns 404 on FRED | Restore live fetch; cover the full 1982-2021 paper window with one control series | `TB4WK`/`GS1M` (true 1-month, but only from 2001-07); defer | User/agent |
| 2026-06-09 | Bridge single-month interior CPI gaps by log-linear interpolation, flagged in `cpi_imputed` (superseded for current-monitoring presentation by the 2026-07-16 scenario decision below) | The canceled 2025-10 CPI release left a permanent FRED hole that froze the complete strict signal at 2025-09. The original 2028-11 recovery estimate was incomplete because it omitted the second YoY hole and TINF warm-up. | Linear interpolation; relaxing rolling `min_periods`; leaving the signal stale | User/agent |
| 2026-06-24 | Trader research mode = **descriptive + rates-only** (the six approved FRED rate series), surfaced as a new **Trader Research** Streamlit tab; the shelved `build_trader_report`/`REGIME_PLAYBOOK` layer stays un-wired | Keep the project descriptive / non-trading and inside the approved market registry; deliver a current-state-conditioned base-rate reading by reusing the Phase 4 linkage tables | Predictive (out-of-sample scoring) or tradable (PnL/sizing); expanding the asset universe within FRED or via an ETF vendor; wiring or deleting the trader layer | User |
| 2026-07-16 | Preserve official October 2025 headline/core CPI as null and add an explicitly unofficial ex-post current-monitoring bundle: `low = September`, `base = sqrt(September * November)`, `high = November`. Build observed and scenario views from one full raw warm-up superset; classify scenarios against prior observed-only eligible history; stability requires agreement across all scenarios. Historical validation, benchmarks, robustness, market linkage, analog populations, and paper reconstruction remain observed-only. | Restore a current descriptive CPI/TINF reading without laundering an estimate into official history or historical evidence. The strict default shifted-36M signal naturally recovers in stages: baseline/epsilon 2029-11, TINF4 2030-02, TINF8 2030-06, and TINF12/complete snapshot 2030-10. | Strict observed-only current display; one geometric central estimate without scenarios; partial rolling windows; one-sided or state-space nowcasting | User |
| 2026-07-16 | Enforce exact 2025-09/2025-11 official calendar endpoints independently for headline/core, separate official value authority from H5 release-time precision, fail the two-series bundle closed on any defect, require the common warm-up authority, and mark pre-October-2025 samples `not_applicable`. Use one canonical observed-only eligibility predicate across every historical consumer. | Prevent row-adjacency substitution, partial-series classifications, estimated-history leakage, sample-trimmed scenario reconstruction, and misleading Paper Window scenario stability while preserving conservative timing provenance. | Accept physical adjacency; permit headline-only current output; duplicate historical guards; show no-op Paper scenarios | User/agent |
| 2026-08-26 | H2 common-sample policy: headline benchmark metrics, ranks, robustness verdicts, cards, and report sentences are computed on one universal common-origin panel per horizon, shared by all five registered models. Native per-model samples are retained only as a labelled coverage diagnostic. An empty universal panel fails explicitly; a sample where no model can forecast returns empty with a disclosed reason. Grid cells that produce no scored row are disclosed rather than dropped. As part of the same change, `beats`/`wins` claim language is replaced everywhere by neutral point-estimate wording (`lower_mae_than_<model>`, `tinf_lowest_mae`, `*_differential_*_pp`, "lower point estimate"); significance remains H10's scope. | Models became available at different dates (walk-forward regime warm-up made TINF/regime the narrowest sample in 35 of 35 evaluable settings), so raw cross-model ranks confounded forecast skill with origin-set difficulty, holding TINF's own metric fixed while every rival was scored on a larger, harder sample. The distortion ran in both directions: `max_history`/`rolling_36_shifted` TINF lowest-RMSE rate fell 0.60 -> 0.00 and lowest-MAE 0.60 -> 0.20 under common origins, while `live_dashboard`/`expanding_shifted` lower-MAE-than-no-change rose 0.40 -> 1.00. Fair comparison is not significance, so the claim language had to go with it. | Pairwise-only intersections (intransitive ranks); relaxing model warm-ups to widen coverage; imputing missing forecasts; keeping native ranks with a caveat; deferring the language change to H10 | User |
| 2026-08-27 | Add `unconditional_drift` as the sixth registered benchmark: `y_t` plus the expanding mean of all prior completed horizon-specific CPI YoY changes, with the same minimum-history gate as the existing pooled TINF fallback. Preserve the TINF branch rule and expose forecast source plus selected, pooled, and same-regime counts. Include drift in H2 common-origin scoring, report comparisons, and robustness point-estimate summaries. | The pooled fallback was previously embedded inside the TINF label, so the incremental contribution of regime conditioning could not be compared with the estimator TINF actually used on fallback rows. Drift availability contains TINF availability, preserving every legacy common-origin panel while making the fallback auditable. | Change TINF to pure-bucket-only; remove fallback; use shrinkage; defer the explicit benchmark until H10. H10 inference wiring, Holm-family construction, and pending H10 xfails remain unchanged. | User/agent |

# Optional but recommended. The project falls back to public FRED CSVs without a key.
# Keep real keys only in .env, never in notebooks or Git.
FRED_API_KEY=

# App defaults are not set via environment variables:
# - date ranges come from the named sample modes in src/transitory_inflation/config.py;
# - the baseline method is chosen in the Streamlit sidebar (default: rolling_36_shifted).

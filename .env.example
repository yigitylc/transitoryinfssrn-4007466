# Optional. The project can fetch public FRED CSVs without a key.
# Keep real keys only in .env, never in notebooks or Git.
FRED_API_KEY=

# App defaults
# Date ranges are selected via named sample modes in src/transitory_inflation/config.py,
# not via environment variables.
BASELINE_METHOD=rolling_36_shifted
